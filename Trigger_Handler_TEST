/* Generated by TestGen on Mon Oct 3 2022 at 3:50 PM (GMT) */
@isTest
private class Trigger_Handler_TEST{
    
    
    @isTest
    private static void createCase_TEST(){
       Account Acc = new Account(Name='TestAcc');
        Contact Con = new Contact(FirstName='Test',LastName='Con',AccountId=Acc.Id,Level__c=System.Label.Contact_Primary);
        Test.startTest();
        insert Acc;
        insert Con;
        List<Contact> var1 = new List<Contact>{new Contact()};
        Trigger_Handler.createCase(var1);
        Test.stopTest();
    }
    @isTest
    private static void createTask_TEST(){
        Account Acc = new Account(Name='TestAcc');
        Contact Con = new Contact(FirstName='Test',LastName='Con',AccountId=Acc.Id,Level__c=System.Label.Contact_Primary);
        Case cas = new Case(ContactId=Con.Id,AccountId=Acc.Id,Priority=System.Label.Priority_High);
        Test.startTest();
        insert Acc;
        insert Con;
        insert cas;    
        List<Case> var1 = new List<Case>{new Case()};
        
        Trigger_Handler.createTask(var1);
        Test.stopTest();
    }
    @isTest
    private static void Trigger_Handler_TEST(){
        Trigger_Handler obj = new Trigger_Handler();
    }
}
